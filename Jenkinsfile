pipeline {
    agent any
    stages {
        stage(Working with variables){
            steps {
                script {
                    val1 = 20
                   println "my val1 is ${val1}"
                }
            }
        }
    }
}
